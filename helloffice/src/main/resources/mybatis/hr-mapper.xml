<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="hr">
	<select id="getTeamList" resultType="memberDto">
		SELECT * FROM HR
		WHERE RESIGN_YN = 'N'
	</select>
	
	<select id="getRepTeamList" resultType="memberDto">
		SELECT * FROM HR
		WHERE RESIGN_YN = 'N'
		AND DEP_NO = '대표'
	</select>
	
	<select id="getMarkTeamList" resultType="memberDto">
		SELECT * FROM HR
		WHERE RESIGN_YN = 'N'
		AND DEP_NO = '마케팅'
	</select>
	
	<select id="getSalesTeamList" resultType="memberDto">
		SELECT * FROM HR
		WHERE RESIGN_YN = 'N'
		AND DEP_NO = '영업'
	</select>
	
	<select id="getDesignTeamList" resultType="memberDto">
		SELECT * FROM HR
		WHERE RESIGN_YN = 'N'
		AND DEP_NO = '디자인'
	</select>
	
	<select id="getDeptList" resultType="deptDto">
		SELECT * FROM DEPARTMENT
		WHERE DEP_DEL = 'N'
	</select>
	
	<select id="cntDepName" resultType="int">
		SELECT COUNT(*) 
		FROM DEPARTMENT
		WHERE DEP_NAME = #{depName} and DEP_DEL = 'N'
	</select>
	
	<insert id="insertDept" parameterType="deptDto">
		INSERT INTO 
		DEPARTMENT (
					DEP_NO, 
					DEP_NAME
		) VALUES (
					SEQ_DEPARTMENT_DEP_NO.nextval, 
					#{depName}
		)
	</insert>
	
	
</mapper>
